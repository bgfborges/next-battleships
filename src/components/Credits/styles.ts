import styled from 'styled-components'

export const CreditsContainer = styled.div`
    width: 100%;
    height: 40px;
    text-align: center;
    justify-self: center;

    span {
        color: var(--red);
    }

    svg {
        margin-bottom: -4px;
    }
`;